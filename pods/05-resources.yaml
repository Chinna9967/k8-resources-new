apiVersion: v1
kind: Pod
metadata:
  name: resources-sample
spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      requests: # softlimit
        cpu: "100m"
        memmory: "128Mi"
      limits:  # hardlimit
        cpu: "200m"
        memory: "256Mi"